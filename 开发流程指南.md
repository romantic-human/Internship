# 开发流程指南

> 本文以已完成的**登录接口（POST /api/user/login）**为案例，拆解「一人全栈一个模块」的标准开发流程。
> 后面每个人开发自己的模块时，照这个流程来就行。

---

## 1. 一句话原则

> **对着设计文档写代码，后端写完自己调通，再写前端，零联调。**

所有接口的 URL、入参、返回值都在《组织架构模块设计方案.md》第 5 章里定义好了，不需要讨论、不需要猜。

---

## 2. 完整流程（6 步）

```
① 认领模块，建分支
       │
       ▼
② 写后端 Model（models.py）
       │
       ▼
③ 写后端 Serializer + View（serializers.py + views.py）
       │
       ▼
④ 后端自测（Swagger / curl）
       │
       ▼
⑤ 写前端页面 + API 调用
       │
       ▼
⑥ commit → push → 提 PR 到 develop
```

---

## 3. 实际案例：登录接口全流程拆解

### 第一步：认领模块，建分支

```bash
git checkout develop
git pull origin develop
git checkout -b feature/auth
```

### 第二步：写后端 Model（`models.py`）

对照设计文档 3.3.1 节的用户表字段，写 Django Model：

```python
# Internship-core/apps/user/models.py

from django.db import models
from django.contrib.auth.models import AbstractBaseUser, BaseUserManager
import bcrypt

class User(AbstractBaseUser):
    """用户表 — 对应设计文档 3.3.1 sys_user"""

    USERNAME_FIELD = "username"
    objects = UserManager()

    username = models.CharField(max_length=64, unique=True)
    nickname = models.CharField(max_length=64, blank=True, default="")
    email = models.EmailField(max_length=128, blank=True, default="")
    # ... 其余字段与设计文档 3.3.1 一一对应

    def set_password(self, raw_password):
        self.password = bcrypt.hashpw(
            raw_password.encode(), bcrypt.gensalt()
        ).decode()

    def check_password(self, raw_password):
        return bcrypt.checkpw(
            raw_password.encode(), self.password.encode()
        )
```

**关键点**：Model 字段名与设计文档的建表 SQL 字段名完全一致，前端不用猜。

### 第三步：写后端 Serializer + View

**Serializer（`serializers.py`）**——定义接收什么参数：

```python
class LoginSerializer(serializers.Serializer):
    username = serializers.CharField(required=True)
    password = serializers.CharField(required=True)
```

**View（`views.py`）**——定义业务逻辑，对照设计文档 5.2 节：

```python
@action(detail=False, methods=["post"], permission_classes=[AllowAny])
def login(self, request):
    """
    对照设计文档 5.2 节：
    Request:  { "username": "admin", "password": "admin123" }
    Response: { "code": 200, "data": { "access_token", "refresh_token", "user" } }
    """
    # 1. 参数校验
    serializer = LoginSerializer(data=request.data)
    serializer.is_valid(raise_exception=True)

    # 2. 查找用户
    user = User.objects.filter(
        username=serializer.validated_data["username"]
    ).first()

    # 3. 验密码
    if not user or not user.check_password(...):
        return APIResponse.error(message="用户名或密码错误", code=3000)

    # 4. 签 JWT
    refresh = RefreshToken.for_user(user)

    # 5. 按设计文档格式返回
    return APIResponse.success(data={
        "access_token": str(refresh.access_token),
        "refresh_token": str(refresh),
        "user": { "id": user.id, "username": user.username, ... }
    })
```

### 第四步：后端自测

```bash
# 先创建测试用户（用 Django shell）
python manage.py shell
>>> from apps.user.models import User
>>> user = User(username="admin", is_superuser=True, status=1)
>>> user.set_password("admin123")
>>> user.save()

# 启动后端
python manage.py runserver 8000
```

打开 http://localhost:8000/api/docs/ ，找到 `POST /api/user/login`，点 "Try it out"：

```json
// 输入
{ "username": "admin", "password": "admin123" }

// 预期输出
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "user": { "id": 1, "username": "admin", ... }
  }
}
```

**Swagger 上跑通了，后端就算完成。** 不需要等前端。

> 也可以用 curl：
> ```bash
> curl -X POST http://localhost:8000/api/user/login \
>   -H "Content-Type: application/json" \
>   -d '{"username":"admin","password":"admin123"}'
> ```

### 第五步：写前端页面

**API 调用（`src/api/user.ts`）**：

```typescript
export function login(data: { username: string; password: string }) {
  return request.post("/user/login", data);
}
// ↑ URL 与设计文档 5.2 节完全一致
```

**登录页面（`src/views/login/Login.vue`）**：

```vue
<template>
  <el-form @keyup.enter="handleLogin">
    <el-input v-model="form.username" placeholder="用户名" />
    <el-input v-model="form.password" type="password" placeholder="密码" />
    <el-button type="primary" @click="handleLogin">登录</el-button>
  </el-form>
</template>

<script setup>
import { useAuthStore } from "@/store/auth";
import { useRouter } from "vue-router";

const authStore = useAuthStore();
const router = useRouter();

async function handleLogin() {
  await authStore.login(form.username, form.password);
  router.push("/");  // 跳转首页
}
</script>
```

### 第六步：提交 PR

```bash
git add .
git commit -m "feat(auth): 实现用户登录接口"
git push origin feature/auth
# → 去 GitHub 提 PR 到 develop
```

---

## 4. 开发节奏总结

| 步骤 | 花多久 | 产出 |
|------|--------|------|
| 读设计文档 | 10 分钟 | 知道你模块有哪些接口、什么字段 |
| 写 Model | 15 分钟 | 一张表 |
| 写 Serializer + View | 30 分钟 | 所有 CRUD 骨架 |
| 后端自测 | 10 分钟 | Swagger 上跑通 |
| 写前端页面 | 30 分钟 | Vue 页面 + API 调用 |
| 前端自测 | 5 分钟 | 浏览器点一遍 |
| **合计** | **约 2 小时** | **一个完整可用的功能** |

---

## 5. 三个强制约定

1. **一切以设计文档为准。** URL、字段名、响应格式不允许自行发明。文档没写清的点在群里问组长，确认后更新文档再改代码。

2. **后端写完先在 Swagger 上自测。** 不要写完后端就丢给前端，Swagger 上一目了然。

3. **分支从 develop 拉，PR 往 develop 提。** 不要直接 push main 或 develop。

---

## 6. 常见错误对照

| 错误 | 正确做法 |
|------|---------|
| URL 写成 `/api/login` | 设计文档写的是 `/api/user/login` |
| 返回格式是 `{ "success": true }` | 设计文档是 `{ "code": 200, "message": "...", "data": {} }` |
| 字段名用驼峰 | 后端用下划线（`access_token`），前端 TS 可以驼峰但请求体必须一致 |
| 不写 Swagger 自测 | 后端 Swagger 跑通再写前端，省去双方联调 |
| 密码存明文 | 必须 bcrypt 加密（参考 models.py 的 set_password） |

---

> **记住**：你只负责自己的模块。接口文档已经定义了输入输出，它就是你和前端的「合同」。你按合同交货，别人按合同接货，不需要再开会。