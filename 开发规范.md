# 开发规范

> 团队协作开发规范，所有人必须遵守。

---

## 1. 分支管理

### 1.1 分支策略（Git Flow 简化版）

```
main               ← 生产分支，只接受 merge，禁止直接 push
  └── develop       ← 开发主分支，日常集成
        ├── feature/user-login     ← 功能分支
        ├── feature/role-mgmt
        └── fix/xxx                ← 修复分支
```

### 1.2 命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 功能分支 | `feature/<模块>-<简述>` | `feature/user-login` |
| 修复分支 | `fix/<简述>` | `fix/token-expire-bug` |
| 紧急修复 | `hotfix/<简述>` | `hotfix/sql-injection` |

### 1.3 工作流程

```bash
# 1. 从 develop 拉取最新代码
git checkout develop
git pull origin develop

# 2. 创建功能分支
git checkout -b feature/your-module

# 3. 开发 + 提交
git add .
git commit -m "feat: 实现用户登录接口"

# 4. 推送到远程（定期 push，防止代码丢失）
git push origin feature/your-module

# 5. 完成后发起 Pull Request → develop
```

---

## 2. Commit 规范

### 2.1 格式

```
<type>(<scope>): <subject>

# 示例
feat(user): 实现登录和 Token 刷新
fix(role): 修复角色删除时关联未清理的问题
docs: 更新 API 文档
```

### 2.2 Type 类型

| Type | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 Bug |
| `docs` | 文档变更 |
| `style` | 代码格式（不影响功能） |
| `refactor` | 重构 |
| `test` | 添加测试 |
| `chore` | 构建/工具/依赖变更 |

### 2.3 Scope（可选）

| Scope | 说明 |
|-------|------|
| `user` | 用户模块 |
| `role` | 角色模块 |
| `menu` | 菜单模块 |
| `permission` | 权限模块 |
| `department` | 部门模块 |
| `log` | 日志模块 |
| `config` | 系统配置模块 |
| `auth` | 认证/授权 |
| `api` | 接口公共层 |
| `frontend` | 前端通用 |

---

## 3. 代码规范

### 3.1 Python（后端）

- 遵循 **PEP 8** 规范
- 使用 4 空格缩进
- 类名：`PascalCase`（如 `UserViewSet`）
- 函数/变量：`snake_case`（如 `get_user_list`）
- 注释使用中文，但变量名和函数名用英文
- 每个函数/类写 docstring 说明用途

### 3.2 TypeScript / Vue（前端）

- 使用 2 空格缩进
- 组件文件：`PascalCase`（如 `UserList.vue`）
- TS 文件/函数：`camelCase`（如 `getUserList`）
- 类型定义优先，避免滥用 `any`
- 组件 props 必须显式声明类型

### 3.3 接口 URL 命名

- 全部小写，单词用连字符 `-` 或用路径层级
- 遵循设计文档第 5 章的接口定义，**不允许自行发明 URL**

---

## 4. 文件命名

### 后端

```
apps/user/
├── __init__.py
├── models.py         ← 数据模型
├── serializers.py    ← 序列化器
├── views.py          ← 视图
├── urls.py           ← 路由
└── tests.py          ← 测试（后续补充）
```

### 前端

```
src/views/system/user/
├── UserList.vue      ← 列表页
├── UserForm.vue      ← 新增/编辑弹窗
└── types.ts          ← 本模块的类型定义
```

---

## 5. 任务分工（建议）

| 模块 | 后端负责人 | 前端负责人 | 优先级 |
|------|-----------|-----------|--------|
| 用户模块 + 认证 | 待分配 | 待分配 | 🔴 最高 |
| 角色模块 | 待分配 | 待分配 | 🔴 最高 |
| 菜单模块 | 待分配 | 待分配 | 🟡 高 |
| 权限模块 | 待分配 | 待分配 | 🟡 高 |
| 部门模块 | 待分配 | 待分配 | 🟢 中 |
| 日志模块 | 待分配 | 待分配 | 🟢 中 |
| 系统配置模块 | 待分配 | 待分配 | ⚪ 低 |

> **建议开发顺序**：用户+认证 → 菜单 → 角色 → 权限 → 部门 → 日志 → 系统配置
>
> 认证是整个系统的入口，用户和菜单是 RBAC 的基础，必须最先完成。

---

## 6. 联调规范

- **接口定义以设计文档为准**，任何变更必须先更新文档再改代码
- 后端接口就绪后，在 Swagger 文档 (`/api/docs/`) 上可直接测试
- 前端开发时优先使用 Swagger 文档 + 后端真实接口联调，不要大量 Mock
- 遇到接口不匹配 → 在开发群提出来，由**双方确认后更新设计文档**

---

> **记住**：《组织架构模块设计方案.md》是唯一真相来源。所有接口、字段、业务规则都以它为准。
