# 环境搭建指南

> 适用于组织架构模块前后端项目的开发环境搭建。按顺序执行即可。

---

## 1. 前置依赖

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Python | 3.11+ | 后端运行环境 |
| Node.js | 18.x+ | 前端运行环境 |
| MySQL | 8.0+ | 数据库 |
| Redis | 7.x | 缓存 / Token 黑名单 |
| Git | 2.x | 版本控制 |
| pnpm | 8.x+ | 前端包管理器（`npm i -g pnpm`） |

---

## 2. 克隆项目

```bash
git clone https://github.com/romantic-human/Internship.git
cd Internship
```

项目结构：
```
Internship/
├── Internship-core/     ← Django 后端
├── Internship-Pre/      ← Vue3 前端
├── 组织架构模块设计方案.md
├── 环境搭建指南.md          ← 本文件
├── 开发规范.md
└── README.md
```

---

## 3. 后端搭建（Django）

### 3.1 创建虚拟环境

```bash
cd Internship-core

# Windows
python -m venv venv
venv\Scripts\activate

# Mac / Linux
python3 -m venv venv
source venv/bin/activate
```

### 3.2 安装依赖

```bash
pip install -r requirements.txt
```

> **注意**：`mysqlclient` 在 Windows 上需要 Visual C++ Build Tools。安装失败可改用 `pip install pymysql`，然后在 `config/settings.py` 顶部加一行 `import pymysql; pymysql.install_as_MySQLdb()`。

### 3.3 配置环境变量

```bash
# 复制示例配置文件
copy .env.example .env      # Windows
cp .env.example .env        # Mac / Linux

# 编辑 .env，填入你的数据库密码等信息
```

### 3.4 创建数据库

登录 MySQL 执行：

```sql
CREATE DATABASE internship_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

然后执行建表 SQL（可选——Django migrate 也会自动建表）：

```bash
# 方式一：用 Django 自动建表（推荐）
python manage.py makemigrations
python manage.py migrate

# 方式二：手动执行 SQL 脚本
mysql -u root -p internship_db < sql/init.sql
```

### 3.5 创建超级管理员

```bash
python manage.py createsuperuser
# 按提示输入用户名、邮箱、密码
```

### 3.6 启动后端

```bash
python manage.py runserver 8000
```

验证：访问 http://localhost:8000/api/docs/ 应该看到 Swagger 文档页面。

---

## 4. 前端搭建（Vue3）

### 4.1 安装依赖

```bash
cd Internship-Pre
pnpm install
```

### 4.2 配置环境变量

```bash
# 复制示例配置文件
copy .env.example .env      # Windows
cp .env.example .env        # Mac / Linux
```

### 4.3 启动前端

```bash
pnpm dev
```

验证：访问 http://localhost:3000 应该看到登录页面。

---

## 5. 前后端联调

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | `http://localhost:3000` | Vue3 开发服务器 |
| 后端 | `http://localhost:8000` | Django 开发服务器 |
| API 文档 | `http://localhost:8000/api/docs/` | Swagger UI |
| API Schema | `http://localhost:8000/api/schema/` | OpenAPI JSON |

> 前端已配置 Vite 代理，`/api` 开头的请求会自动转发到 `http://localhost:8000`。

---

## 6. 常见问题

### mysqlclient 安装失败（Windows）

```bash
# 改用 pymysql
pip install pymysql
```

然后在 `config/settings.py` 文件最顶部添加：
```python
import pymysql
pymysql.install_as_MySQLdb()
```

### Redis 连接失败

确认 Redis 服务已启动：
```bash
redis-cli ping
# 应返回 PONG
```

如果本地没有 Redis，可暂时注释掉 `settings.py` 中的 `CACHES` 配置（只影响缓存功能）。

### 前端请求 401

1. 确认后端已启动 (`python manage.py runserver 8000`)
2. 确认前端代理配置正确（`vite.config.ts` 中 `proxy` 指向 `localhost:8000`）
3. 如果 Swagger 文档可访问但前端 401，检查 `.env` 中 `VITE_API_BASE_URL`

---

> **下一步**：阅读《开发规范.md》了解分支策略和编码约定后即可开始开发。