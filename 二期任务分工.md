# 二期任务分工（3 人全栈）

> 基于一期代码审查，梳理所有未完善功能，按**模块归属**和**工作量均衡**原则分配给 3 名全栈开发。

---

## 任务总览

| 人员 | 负责方向 | 任务数 | 预估工时 |
|------|---------|:------:|:--------:|
| **A** | 用户 + 认证基础设施 | 5 | 3-4 天 |
| **B** | 角色 + 权限 + 菜单 | 5 | 3-4 天 |
| **C** | Dashboard + 日志 + 配置 + 通用 | 6 | 3-4 天 |

---

## 人员 A：用户管理 + 认证基础

> 负责 `user` 模块的全栈补全，以及全局认证机制完善。

### 任务 A-1：用户导出 / 导入 Excel ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `apps/user/views.py` 的 `export` 和 `import_` action 当前返回 `"待实现"`，需用 `openpyxl` 实现 Excel 导出/导入 |
| 前端 | `UserList.vue` 增加「导出」按钮 + 「导入」上传弹窗，调用 `GET /api/user/export` 和 `POST /api/user/import` |
| 参考 | `apps/role/views.py` 的 `export` 已实现 CSV 导出，可参考 |

### 任务 A-2：用户列表搜索过滤 ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `apps/user/views.py` 的 `list` 方法，支持 `username`、`status`、`department_id` 查询参数过滤 |
| 前端 | `UserList.vue` 已有搜索 UI，确保参数正确传递（当前已传但后端忽略） |

### 任务 A-3：Token 无感刷新 ⭐高

| 项 | 内容 |
|----|------|
| 前端 | `utils/request.ts` 的 axios 拦截器中，token 过期时调用 `refreshTokenApi()` 自动刷新，而非跳转登录 |
| 说明 | `refreshTokenApi` 已在 `api/user.ts` 定义，`setTokens` 已在 `store/auth.ts` 定义，只需在拦截器中串联逻辑 |

### 任务 A-4：用户表单增加密码字段 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `UserForm.vue` 新建用户时增加密码输入框，传递 `password` 字段给后端 |
| 后端 | 确认 `UserCreateSerializer` 支持 `password` 参数（默认 123456，允许自定义） |

### 任务 A-5：用户批量删除 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `UserList.vue` 增加表格复选框 + 「批量删除」按钮，调用 `DELETE /api/user/batch` |
| 前端 API | `api/user.ts` 补充 `batchDeleteUsers` 函数 |

---

## 人员 B：角色 + 权限 + 菜单

> 负责 `role`、`permission`、`menu` 模块的全栈补全。

### 任务 B-1：修复角色列表搜索 ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `apps/role/views.py` 的 `list` 方法，支持 `role_name`、`status` 查询参数过滤 |
| 前端 | `RoleList.vue` 已有搜索 UI，确保参数正确传递 |

### 任务 B-2：角色导出按钮 ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `GET /api/role/export` 已实现 ✅ |
| 前端 | `RoleList.vue` 增加「导出」按钮，调用 `api/role.ts` 已定义的 `exportRoles()` 函数，下载文件 |

### 任务 B-3：权限-菜单绑定 UI ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `GET/PUT /api/permission/:id/menus` 已实现 ✅ |
| 前端 | `PermissionList.vue` 的 `handleBindMenu` 当前只显示占位消息，需改为弹窗展示菜单树勾选界面 |
| 前端 API | `api/permission.ts` 已定义 `getPermissionMenus()` / `bindPermissionMenus()`，直接调用 |

### 任务 B-4：角色批量删除 + 权限/菜单列表搜索 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `RoleList.vue` 增加表格复选框 + 「批量删除」按钮，API 补充 `batchDeleteRoles` |
| 后端 | `apps/permission/views.py` 的 `list` 支持 `permission_name`、`status` 过滤 |
| 后端 | `apps/menu/views.py` 的 `list` / `tree` 支持 `menu_name` 关键词搜索 |

### 任务 B-5：按钮级权限控制 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `directives/permission.ts` 已定义 `v-permission` 指令，但**没有任何页面使用** |
| 任务 | 在 `UserList`、`RoleList`、`MenuTree`、`DeptTree`、`PermissionList`、`LogList`、`ConfigList` 的操作按钮上添加 `v-permission` 指令 |

---

## 人员 C：Dashboard + 日志 + 配置 + 通用

> 负责 `dashboard`、`log`、`config` 模块补全，以及跨模块通用功能。

### 任务 C-1：Dashboard 图表 ⭐高

| 项 | 内容 |
|----|------|
| 前端 | `Dashboard.vue` 引入 ECharts，添加**用户角色分布饼图** + **近7天日志趋势折线图** |
| 后端 | `apps/dashboard/views.py` 补充 `GET /api/dashboard/trend` 接口，返回近 7 天每日日志数量数组 |
| 依赖 | 前端需安装 `echarts`：`pnpm add echarts` |

### 任务 C-2：日志导出按钮 ⭐高

| 项 | 内容 |
|----|------|
| 后端 | `GET /api/log/export` 已实现 ✅ |
| 前端 | `LogList.vue` 增加「导出」按钮，调用 `api/log.ts` 已定义的 `exportLogs()` 函数，下载文件 |

### 任务 C-3：修复日志模块 destroy 方法 🔹中

| 项 | 内容 |
|----|------|
| 后端 | `apps/log/views.py` 使用 `ReadOnlyModelViewSet`，其 `destroy` 方法不可达。需改为 `ModelViewSet` 或通过 router 额外注册 DELETE 路由 |

### 任务 C-4：日志时间范围筛选 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `LogList.vue` 增加日期范围选择器（`el-date-picker` type="daterange"），传递 `start_date` / `end_date` 参数 |
| 后端 | `apps/log/views.py` 的 `list` 支持 `start_date`、`end_date` 过滤 |

### 任务 C-5：配置/部门/菜单搜索 🔹中

| 项 | 内容 |
|----|------|
| 前端 | `ConfigList.vue`、`DeptTree.vue`、`MenuTree.vue` 各增加搜索框 |
| 后端 | `apps/config_app/views.py` 支持 `config_name` / `config_key` 过滤；部门/菜单搜索同 B-4 协作 |

### 任务 C-6：404 页面 + 表单验证完善 ⚪低

| 项 | 内容 |
|----|------|
| 前端 | 新建 `views/NotFound.vue`，`router/index.ts` 通配路由指向 404 而非 redirect dashboard |
| 前端 | `DeptForm.vue` 手机号正则校验、邮箱格式校验；`ConfigForm.vue` JSON 类型配置值格式校验 |

---

## 跨人员协作事项

| 事项 | 涉及人员 | 说明 |
|------|---------|------|
| 后端权限校验补全 | A + B | `UserViewSet` 和 `RoleViewSet` 需补充 `permission_key` + `get_permissions()`，A 改 user、B 改 role |
| 部门/菜单搜索 | B + C | B 负责后端过滤接口，C 负责前端搜索 UI，需约定参数名 |

---

## 开发规范提醒

1. **分支策略**：每人在自己的 feature 分支上开发（如 `feature/xxx`），无需再建子分支
2. **提交格式**：Conventional Commits — `feat(user): implement Excel export`
3. **PR 流程**：commit → rebase develop → push → 提 PR（base: develop）
4. **TypeScript**：所有前端代码**禁止使用 `any`**，必须使用已定义的 interface 类型
5. **不改动**：已有接口签名和数据库表结构不允许修改，只能新增

---

## 验收标准

每个任务完成后需满足：

- [ ] 后端接口可正常响应（Postman / curl 验证）
- [ ] 前端页面可正常操作（无控制台报错）
- [ ] 代码无 `<<<<<<<` / `=======` / `>>>>>>>` 冲突标记
- [ ] PR 无冲突，可通过 GitHub 合并
- [ ] TypeScript 无 `any` 类型（前端）
