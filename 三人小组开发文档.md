# 企业智能分析平台 — 三人小组开发文档

> **版本**：V1.0  
> **更新日期**：2026 年 6 月  
> **仓库地址**：https://github.com/romantic-human/Internship

---

## 一、项目概述

### 1.1 项目简介

企业智能分析平台是一个基于 Vue3 + Django 的前后端分离后台管理系统，提供用户、角色、菜单、权限、部门、日志、系统配置等基础管理能力，并集成 RAG 知识库（AI 问答）等 AI 模块。

### 1.2 系统架构

```
┌─ 前端（Vue3 + TypeScript + Element Plus + Pinia）─┐
│  Vite 开发服务器 :3000                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 22 个页面 │ │ 10 个API │ │ 动态路由  │           │
│  └──────────┘ └──────────┘ └──────────┘           │
└──────────────── /api 代理 ─────────────────────────┘
                        ↓
┌─ 后端（Django 4.2 + DRF + SimpleJWT）─────────────┐
│  Gunicorn :8000                                    │
│  ┌──────────────────────────────────────────┐      │
│  │ 9 个业务模块 · 68+ API 接口 · JWT 认证    │      │
│  └──────────────────────────────────────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │  MySQL   │ │  Redis   │ │ ChromaDB │           │
│  └──────────┘ └──────────┘ └──────────┘           │
└────────────────────────────────────────────────────┘
```

### 1.3 技术栈清单

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **前端框架** | Vue 3 | 3.4+ | Composition API + `<script setup>` |
| **类型系统** | TypeScript | 5.3+ | 严格模式 |
| **构建工具** | Vite | 5.1+ | 热更新 + API 代理 |
| **UI 组件库** | Element Plus | 2.5+ | 含图标库 |
| **状态管理** | Pinia | 2.1+ | auth store + app store |
| **路由** | Vue Router | 4.3+ | 静态路由 + 动态路由（菜单驱动） |
| **HTTP 客户端** | Axios | 1.6+ | 含 Token 自动刷新拦截器 |
| **图表** | ECharts | 6.1+ | 仪表盘统计 |
| **拖拽排序** | SortableJS | 1.15+ | 树形/列表排序 |
| **后端框架** | Django | 4.2 | LTS 版本 |
| **REST API** | DRF | 3.14 | ViewSet + Serializer |
| **认证** | SimpleJWT | 5.3 | Access 120min / Refresh 7天 |
| **数据库** | MySQL 8.0+ | — | 主数据库（SQLite3 开发备选） |
| **缓存** | Redis | 5.0+ | 会话/缓存 |
| **向量库** | ChromaDB | — | RAG 文档向量存储 |
| **API 文档** | drf-spectacular | 0.27 | OpenAPI/Swagger |
| **密码加密** | bcrypt | 4.1 | 替代 Django 默认 hasher |

---

## 二、模块总览与菜单结构

### 2.1 目标菜单结构（对应截图）

```
仪表盘                         ← 首页统计面板
系统管理（目录）
  ├── 用户管理                 ← 用户 CRUD + 导入导出
  ├── 角色管理                 ← 角色 CRUD + 菜单/用户分配
  ├── 菜单管理                 ← 树形菜单 CRUD + 拖拽排序
  ├── 字典管理                 ← 数据字典 CRUD（待开发）
  ├── 权限管理                 ← 权限 CRUD + 菜单绑定
  ├── 部门管理                 ← 树形部门 CRUD + 拖拽排序
  ├── 操作日志                 ← 日志查看/导出/清空
  └── 系统配置                 ← 配置管理 + 面板设置
NL2SQL分析（目录，待开发）
  ├── SQL智能查询              ← 自然语言转 SQL + 执行
  ├── 查询历史                 ← 历史查询记录 + 收藏
  └── 数据源管理               ← 数据库连接 + 表结构浏览
RAG知识库（目录）
  ├── 知识库列表               ← 知识库 CRUD
  ├── 文档管理                 ← 文档上传/处理/查看
  └── AI问答                   ← SSE 流式对话
学生中心（目录，待开发）
  ├── 学生列表                 ← 学生信息管理
  └── 成绩管理                 ← 成绩查询与录入
用户中心（目录，待开发）
  ├── 个人资料                 ← 用户自助查看/编辑
  └── 消息通知                 ← 系统通知与待办
```

### 2.2 后端模块一览

| 模块 | Django App | 数据表 | API 端点数 | 状态 |
|------|-----------|--------|-----------|------|
| 用户管理 | `apps.user` | sys_user, sys_user_role_relation | 17 | ✅ 已完成 |
| 角色管理 | `apps.role` | sys_role, sys_role_menu_relation | 14 | ✅ 已完成 |
| 菜单管理 | `apps.menu` | sys_menu | 9 | ✅ 已完成 |
| 权限管理 | `apps.permission` | sys_permission, sys_menu_permission_relation | 10 | ✅ 已完成 |
| 部门管理 | `apps.department` | sys_department | 9 | ✅ 已完成 |
| 操作日志 | `apps.log` | sys_operation_log | 4 | ✅ 已完成 |
| 系统配置 | `apps.config_app` | sys_config | 8 | ✅ 已完成 |
| 仪表盘 | `apps.dashboard` | — (聚合查询) | 1 | ✅ 已完成 |
| RAG知识库 | `apps.rag` | rag_knowledge_base, rag_document, rag_document_chunk | 8 | ✅ 已完成 |
| **NL2SQL分析** | `apps.nl2sql` | nl2sql_datasource, nl2sql_query_history | 6 | 🔶 待开发 |
| **字典管理** | `apps.dict` | sys_dict_type, sys_dict_data | 8 | ✅ 已完成 |
| **学生中心** | `apps.student` | student_info, student_score | 6 | 🔶 待开发 |
| **用户中心** | `apps.user_center` | — (复用 user 模块) | 4 | 🔶 待开发 |

---

## 三、数据库设计

### 3.1 核心表结构（10 张基础表 + 3 张 RAG 表 + 2 张 NL2SQL 表）

```
sys_user ──┬── sys_user_role_relation ── sys_role ── sys_role_menu_relation ── sys_menu
           │                                                                     │
           └── department_id → sys_department                        sys_menu_permission_relation
                                                                              │
                                                                        sys_permission

sys_operation_log（独立表，中间件自动记录）
sys_config（键值对配置表）
rag_knowledge_base → rag_document → rag_document_chunk
nl2sql_datasource（数据源配置表，待开发）
nl2sql_query_history（查询历史表，待开发）
sys_dict_type + sys_dict_data（字典表，已完成）
student_info + student_score（学生表，待开发）
```

### 3.2 关键字段说明

| 表 | 关键字段 |
|---|---------|
| `sys_user` | username(unique), password(bcrypt), department_id(FK), is_superuser |
| `sys_role` | role_name(unique), role_key(unique), role_sort, status |
| `sys_menu` | parent_id(FK→self), menu_type(0目录/1菜单/2按钮), path, component, icon, permission |
| `sys_permission` | permission_name, permission_key(unique) |
| `sys_department` | parent_id(FK→self), dept_name, leader, sort_order |
| `sys_operation_log` | username, module, operation, method, request_url, status, execution_time |
| `sys_config` | config_name, config_key(unique), config_value, config_type(0字符串/1数字/2布尔/3JSON) |

### 3.3 RBAC 权限链路

```
User → UserRoleRelation → Role → RoleMenuRelation → Menu → MenuPermissionRelation → Permission
```

- 超级管理员（`is_superuser=True`）自动拥有 `*:*:*` 全部权限
- 前端通过 `v-permission` 指令 + `authStore.hasPermission()` 双重校验

---

## 四、API 接口清单

### 4.1 用户模块 `/api/user/`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/login` | 登录 | AllowAny |
| POST | `/register` | 注册 | AllowAny |
| POST | `/refresh-token` | 刷新 Token | AllowAny |
| GET | `/` | 用户列表（分页+筛选） | IsAuthenticated |
| POST | `/` | 创建用户 | IsAuthenticated |
| GET | `/{id}` | 用户详情 | IsAuthenticated |
| PUT | `/{id}` | 更新用户 | IsAuthenticated |
| DELETE | `/{id}` | 删除用户 | IsAuthenticated |
| DELETE | `/batch` | 批量删除 | IsAuthenticated |
| PUT | `/{id}/status` | 修改状态 | IsAuthenticated |
| PUT | `/reset-password` | 重置密码 | IsAuthenticated |
| GET | `/profile` | 获取个人资料 | IsAuthenticated |
| PUT | `/profile` | 更新个人资料 | IsAuthenticated |
| PUT | `/update-password` | 修改密码 | IsAuthenticated |
| POST | `/avatar` | 上传头像 | IsAuthenticated |
| GET | `/check-unique` | 唯一性检查 | IsAuthenticated |
| GET | `/template` | 下载导入模板 | IsAuthenticated |
| GET | `/export` | 导出用户 | IsAuthenticated |
| POST | `/import` | 导入用户 | IsAuthenticated |

### 4.2 角色模块 `/api/role/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/` | 角色列表 / 创建 |
| GET/PUT/DELETE | `/{id}` | 详情 / 更新 / 删除 |
| GET | `/all` | 全部角色（下拉用） |
| DELETE | `/batch` | 批量删除 |
| PUT | `/{id}/status` | 修改状态 |
| PUT | `/{id}/sort` | 排序 |
| POST | `/batch-sort` | 批量排序 |
| GET/PUT | `/{id}/menus` | 获取/分配菜单 |
| GET/PUT | `/{id}/users` | 获取/分配用户 |
| GET | `/template` / `/export` | 模板/导出 |
| POST | `/import` | 导入 |

### 4.3 菜单模块 `/api/menu/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/` | 菜单列表 / 创建 |
| GET/PUT/DELETE | `/{id}` | 详情 / 更新 / 删除 |
| GET | `/tree` | 菜单树 |
| PUT | `/{id}/status` | 修改状态 |
| PUT | `/{id}/sort` | 排序 |
| POST | `/batch-sort` | 批量排序 |
| DELETE | `/batch` | 批量删除 |
| GET | `/export` | 导出 |

### 4.4 权限模块 `/api/permission/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/` | 权限列表 / 创建 |
| GET/PUT/DELETE | `/{id}` | 详情 / 更新 / 删除 |
| GET/PUT | `/{id}/menus` | 获取/绑定菜单 |
| DELETE | `/batch` | 批量删除 |
| PUT | `/{id}/status` / `/{id}/sort` | 状态/排序 |
| GET | `/export` | 导出 |

### 4.5 部门模块 `/api/department/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/` | 部门列表 / 创建 |
| GET/PUT/DELETE | `/{id}` | 详情 / 更新 / 删除 |
| GET | `/tree` | 部门树 |
| PUT | `/{id}/status` / `/{id}/sort` | 状态/排序 |
| POST | `/batch-sort` | 批量排序 |
| DELETE | `/batch` | 批量删除 |
| GET | `/export` | 导出 |

### 4.6 日志模块 `/api/log/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 日志列表（只读，支持多条件筛选） |
| GET | `/{id}` | 日志详情 |
| DELETE | `/clear` | 清空日志 |
| GET | `/export` | 导出日志 |

### 4.7 系统配置模块 `/api/config/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/` | 配置列表 / 创建 |
| GET/PUT/DELETE | `/{id}` | 详情 / 更新 / 删除 |
| GET | `/by-key` | 按键获取配置值 |
| GET | `/panel` | 获取面板配置 |
| POST | `/panel-save` | 保存面板配置 |
| PUT | `/{id}/sort` | 排序 |
| POST | `/batch-sort` | 批量排序 |
| POST | `/upload` | 图片上传 |

### 4.8 仪表盘 `/api/dashboard/stats/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 返回 user_count, role_count, menu_count, permission_count, department_count, log_today/week/month, recent_logs |

### 4.9 数据字典 `/api/dict/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/type` | 字典类型列表 / 创建 |
| GET/PUT/DELETE | `/type/{id}` | 类型详情 / 更新 / 删除 |
| PUT | `/type/{id}/status` | 类型状态切换 |
| DELETE | `/type/batch` | 批量删除类型 |
| GET | `/type/export` | 导出字典类型 |
| GET/POST | `/data` | 字典数据列表 / 创建 |
| GET/PUT/DELETE | `/data/{id}` | 数据详情 / 更新 / 删除 |
| PUT | `/data/{id}/status` | 数据状态切换 |
| DELETE | `/data/batch` | 批量删除数据 |
| GET | `/data/type/{dict_type}` | 按类型编码获取全部启用项（下拉框用） |

### 4.10 RAG 知识库 `/api/rag/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/kb/` | 知识库列表 / 创建 |
| GET/PUT/DELETE | `/kb/{id}/` | 知识库详情 / 更新 / 删除 |
| GET | `/documents/` | 文档列表（按 knowledge_base 过滤） |
| POST | `/documents/upload/` | 上传文档 |
| DELETE | `/documents/{id}/` | 删除文档 |
| POST | `/documents/{id}/reprocess/` | 重新处理文档 |
| POST | `/kb/{id}/chat/` | AI 问答（普通） |
| POST | `/kb/{id}/chat-stream/` | AI 问答（SSE 流式） |

### 4.11 NL2SQL 分析 `/api/nl2sql/`（待开发）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/query/` | 自然语言转 SQL 并执行 |
| GET | `/history/` | 查询历史（分页+筛选） |
| PUT | `/history/{id}/favorite/` | 收藏/取消收藏 |
| DELETE | `/history/{id}/` | 删除历史记录 |
| GET/POST | `/datasource/` | 数据源列表 / 创建 |
| GET/PUT/DELETE | `/datasource/{id}/` | 数据源详情 / 更新 / 删除 |
| GET | `/datasource/{id}/tables/` | 获取数据源表结构 |
| POST | `/datasource/{id}/test/` | 测试数据源连接 |

---

## 五、前端页面结构

### 5.1 页面清单（27 个 Vue 组件）

| 页面 | 文件路径 | 说明 |
|------|---------|------|
| 登录 | `views/login/Login.vue` | 账号密码登录 + 记住密码 |
| 仪表盘 | `views/dashboard/Dashboard.vue` | ECharts 统计面板 |
| 用户列表 | `views/system/user/UserList.vue` | 高级筛选 + CRUD + 导入导出 |
| 用户表单 | `views/system/user/UserForm.vue` | 新增/编辑弹窗 |
| 角色列表 | `views/system/role/RoleList.vue` | CRUD + 状态切换 + 导入导出 |
| 角色表单 | `views/system/role/RoleForm.vue` | 新增/编辑弹窗 |
| 菜单管理 | `views/system/menu/MenuTree.vue` | 树形展示 + 拖拽排序 + CRUD |
| 菜单表单 | `views/system/menu/MenuForm.vue` | 新增/编辑弹窗 |
| 权限列表 | `views/system/permission/PermissionList.vue` | CRUD + 菜单绑定 |
| 权限表单 | `views/system/permission/PermissionForm.vue` | 新增/编辑弹窗 |
| 部门管理 | `views/system/department/DeptTree.vue` | 树形展示 + 拖拽排序 + CRUD |
| 部门表单 | `views/system/department/DeptForm.vue` | 新增/编辑弹窗 |
| 操作日志 | `views/system/log/LogList.vue` | 日志查看 + 导出 |
| 配置列表 | `views/system/config/ConfigList.vue` | 高级配置管理 |
| 配置表单 | `views/system/config/ConfigForm.vue` | 配置编辑弹窗 |
| 配置面板 | `views/system/config/ConfigPanel.vue` | 可视化系统设置 |
| 个人中心 | `views/system/profile/Profile.vue` | 资料编辑 + 密码修改 + 头像上传 |
| 知识库列表 | `views/rag/KBList.vue` | 知识库 CRUD |
| 文档管理 | `views/rag/KBDetail.vue` | 文档上传/查看/删除 |
| AI 问答 | `views/rag/ChatView.vue` | SSE 流式对话界面 |
| **SQL智能查询** | `views/nl2sql/QueryView.vue` | 自然语言转 SQL + 结果展示（待开发） |
| **查询历史** | `views/nl2sql/HistoryList.vue` | 历史查询 + 收藏（待开发） |
| **数据源管理** | `views/nl2sql/DataSourceList.vue` | 数据库连接管理（待开发） |
| 403 页面 | `views/error/403.vue` | 无权限提示 |
| 404 页面 | `views/error/404.vue` | 页面不存在提示 |

### 5.2 动态路由机制

```
登录成功 → authStore.fetchUserInfo() → 获取菜单树(menuTree)
    → buildRoutesFromMenu() 递归生成路由
    → router.addRoute() 动态注册
    → componentMap 映射 component 字段到 Vue 组件
```

菜单表 `component` 字段示例：`"system/user/UserList"` → 加载 `views/system/user/UserList.vue`

### 5.3 权限控制

| 方式 | 使用场景 | 示例 |
|------|---------|------|
| `v-permission` 指令 | 按钮级隐藏 | `v-permission="'user:delete'"` |
| `authStore.hasPermission()` | 条件渲染 | `v-if="authStore.hasPermission('user:edit')"` |
| 后端 `HasPermission` | API 级校验 | ViewSet 设置 `permission_key = "user:list"` |

---

## 六、三人分工方案

### 6.0 解耦原则

> **核心规则：谁的模块谁负责全链路**

每个开发人员对自己的模块负责**从后端到前端到菜单注册的完整链路**，不依赖其他人帮忙注册 componentMap / iconMap / 菜单数据。

| 规则 | 说明 |
|------|------|
| 自建 componentMap | 每个新模块的开发者自行在 `store/auth.ts` 中注册自己的组件映射 |
| 自建菜单数据 | 每个新模块的开发者自行准备 SQL 菜单插入语句（或 seed 脚本） |
| 自建 iconMap | 如果用到新图标，开发者自行在 `Layout.vue` 的 iconMap 中添加 |
| 接口约定先行 | 前后端 API 路径在开发前确定并写入本文档，各人按文档开发，无需等待对方 |
| 文件隔离 | 每人只修改自己模块的文件 + 公共注册文件（auth.ts、Layout.vue、init.sql） |
| 公共注册文件冲突预防 | 修改 auth.ts / Layout.vue / init.sql 时只添加自己模块的部分，不改动其他人的代码 |

### 6.1 角色定义

| 角色 | 负责模块 | 核心职责 |
|------|---------|----------|
| **人员 A（系统核心）** | 用户管理、角色管理、仪表盘、用户中心、学生中心 | 认证体系、RBAC 权限模型、数据统计、用户自助、学生管理 |
| **人员 B（系统管理）** | 菜单管理、部门管理、权限管理、操作日志、字典管理 | 树形结构、拖拽排序、日志中间件、数据字典 |
| **人员 C（配置与 AI）** | 系统配置、RAG 知识库、NL2SQL 分析 | 配置面板、知识库、NL2SQL |

### 6.2 详细任务分解

#### 人员 A — 系统核心模块

| 任务 | 前端文件 | 后端文件 | 状态 |
|------|---------|---------|------|
| 登录/注册/Token刷新 | `Login.vue`, `request.ts` | `apps/user/views.py` | ✅ 已完成 |
| 用户 CRUD + 筛选 | `UserList.vue`, `UserForm.vue` | `apps/user/` | ✅ 已完成 |
| 用户导入/导出 | `UserList.vue` | `apps/user/views.py` | ✅ 已完成 |
| 角色 CRUD + 分配 | `RoleList.vue`, `RoleForm.vue` | `apps/role/` | ✅ 已完成 |
| 仪表盘统计 | `Dashboard.vue` | `apps/dashboard/views.py` | ✅ 已完成 |
| 个人中心 | `Profile.vue` | `apps/user/views.py` | ✅ 已完成 |
| JWT 认证拦截器 | `utils/request.ts` | `settings.py` | ✅ 已完成 |
| 统一权限 Store | `store/auth.ts` | — | ✅ 已完成 |
| 学生中心后端 | — | `apps/student/` | 🔶 待开发 |
| 学生中心前端 | `views/student/` | — | 🔶 待开发 |
| ↳ componentMap 注册 | `store/auth.ts`（添加 student/* 映射） | — | 🔶 待开发 |
| ↳ 菜单数据 + iconMap | `sql/init.sql` + `Layout.vue`（图标） | — | 🔶 待开发 |
| 用户中心 | `views/user-center/` | `apps/user/views.py` | 🔶 待开发 |
| ↳ componentMap 注册 | `store/auth.ts`（添加 user-center/* 映射） | — | 🔶 待开发 |
| ↳ 菜单数据 | `sql/init.sql`（添加用户中心菜单） | — | 🔶 待开发 |

> **人员 A 自给自足**：学生中心和用户中心的前端、后端、菜单、componentMap、图标全部由人员 A 独立完成，无需依赖其他人。

#### 人员 B — 系统管理模块

| 任务 | 前端文件 | 后端文件 | 状态 |
|------|---------|---------|------|
| 菜单树 CRUD | `MenuTree.vue`, `MenuForm.vue` | `apps/menu/` | ✅ 已完成 |
| 菜单拖拽排序 | `MenuTree.vue` (SortableJS) | `apps/menu/views.py` | ✅ 已完成 |
| 部门树 CRUD | `DeptTree.vue`, `DeptForm.vue` | `apps/department/` | ✅ 已完成 |
| 部门拖拽排序 | `DeptTree.vue` (SortableJS) | `apps/department/views.py` | ✅ 已完成 |
| 权限 CRUD + 绑定 | `PermissionList.vue`, `PermissionForm.vue` | `apps/permission/` | ✅ 已完成 |
| 操作日志 | `LogList.vue` | `apps/log/` | ✅ 已完成 |
| 日志中间件 | — | `utils/middleware.py` | ✅ 已完成 |
| 动态路由生成 | `store/auth.ts`, `router/index.ts` | — | ✅ 已完成 |
| Layout 侧边栏 | `layout/Layout.vue` | — | ✅ 已完成 |
| 错误页面 | `error/403.vue`, `error/404.vue` | — | ✅ 已完成 |
| 字典管理后端 | — | `apps/dict/` | ✅ 已完成 |
| 字典管理前端 | `views/system/dict/DictList.vue`, `DictForm.vue` | — | ✅ 已完成 |
| ↳ componentMap 注册 | `store/auth.ts`（添加 system/dict/* 映射） | — | ✅ 已完成 |
| ↳ 菜单数据 | `sql/init.sql`（添加字典管理菜单） | — | ✅ 已完成 |

> **人员 B 自给自足**：字典管理的后端、前端、菜单、componentMap 全部由人员 B 独立完成。**不再负责替其他人注册 componentMap 或菜单数据。**

#### 人员 C — 配置与 AI 模块

| 任务 | 前端文件 | 后端文件 | 状态 |
|------|---------|---------|------|
| 系统配置 CRUD | `ConfigList.vue`, `ConfigForm.vue` | `apps/config_app/` | ✅ 已完成 |
| 配置面板 | `ConfigPanel.vue`, `configPanel.ts` | `apps/config_app/views.py` | ✅ 已完成 |
| 图片上传 | `ConfigPanel.vue` | `apps/config_app/views.py` | ✅ 已完成 |
| 知识库列表 | `KBList.vue` | `apps/rag/views.py` | ✅ 已完成 |
| 文档管理 | `KBDetail.vue` | `apps/rag/views.py` | ✅ 已完成 |
| 文档上传/处理 | `KBDetail.vue` | `apps/rag/services/` | ✅ 已完成 |
| AI 问答（SSE 流式） | `ChatView.vue` | `apps/rag/views.py` + `llm_service.py` | ✅ 已完成 |
| 向量存储 | — | `apps/rag/services/vector_store.py` | ✅ 已完成 |
| NL2SQL 后端服务 | — | `apps/nl2sql/` 全部文件 | 🔶 待开发 |
| NL2SQL 前端页面 | `views/nl2sql/` 全部文件 | — | 🔶 待开发 |
| ↳ componentMap 注册 | `store/auth.ts`（添加 nl2sql/* 映射） | — | 🔶 待开发 |
| ↳ iconMap 注册 | `layout/Layout.vue`（添加 NL2SQL 图标） | — | 🔶 待开发 |
| ↳ 菜单 + 权限数据 | `sql/init.sql`（添加 NL2SQL 菜单与权限） | — | 🔶 待开发 |

> **人员 C 自给自足**：NL2SQL 的后端、前端、菜单、componentMap、图标、权限数据全部由人员 C 独立完成。**不再需要人员 B 帮忙注册。**

---

## 七、项目目录结构

```
Internship/
├── Internship-Pre/                  # 前端项目
│   ├── src/
│   │   ├── api/                     # API 接口定义（10 个 .ts 文件）
│   │   │   ├── user.ts              #   用户认证 + 管理
│   │   │   ├── role.ts              #   角色管理
│   │   │   ├── menu.ts              #   菜单管理
│   │   │   ├── permission.ts        #   权限管理
│   │   │   ├── department.ts        #   部门管理
│   │   │   ├── config.ts            #   系统配置
│   │   │   ├── configPanel.ts       #   配置面板
│   │   │   ├── log.ts               #   操作日志
│   │   │   ├── dashboard.ts         #   仪表盘
│   │   │   ├── rag.ts               #   RAG 知识库
│   │   │   └── nl2sql.ts            #   NL2SQL 分析（待开发）
│   │   ├── directives/
│   │   │   └── permission.ts        # v-permission 指令
│   │   ├── layout/
│   │   │   └── Layout.vue           # 主布局（侧边栏 + Header）
│   │   ├── router/
│   │   │   └── index.ts             # 静态路由 + 动态路由守卫
│   │   ├── store/
│   │   │   ├── auth.ts              # 认证 Store（用户/菜单/权限/componentMap）
│   │   │   ├── app.ts               # 应用 Store（侧边栏折叠/主题）
│   │   │   └── index.ts             # Store 入口
│   │   ├── utils/
│   │   │   ├── request.ts           # Axios 封装（拦截器 + Token 刷新）
│   │   │   └── theme.ts             # 主题工具
│   │   ├── views/                   # 页面组件（22 个 .vue 文件）
│   │   │   ├── login/               #   登录
│   │   │   ├── dashboard/           #   仪表盘
│   │   │   ├── system/              #   系统管理（7 个子模块）
│   │   │   ├── rag/                 #   RAG 知识库（3 个页面）
│   │   │   ├── nl2sql/              #   NL2SQL 分析（3 个页面，待开发）
│   │   │   └── error/               #   错误页面
│   │   ├── App.vue
│   │   └── main.ts
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
│
├── Internship-core/                 # 后端项目
│   ├── apps/                        # Django 应用（9 个模块）
│   │   ├── user/                    #   用户认证 + 管理
│   │   │   ├── models.py            #     User, UserRoleRelation
│   │   │   ├── serializers.py       #     序列化器
│   │   │   ├── views.py             #     UserViewSet (17 端点)
│   │   │   └── urls.py
│   │   ├── role/                    #   角色管理
│   │   │   ├── models.py            #     Role, RoleMenuRelation
│   │   │   ├── views.py             #     RoleViewSet (14 端点)
│   │   │   └── urls.py
│   │   ├── menu/                    #   菜单管理
│   │   │   ├── models.py            #     Menu (自引用树)
│   │   │   ├── views.py             #     MenuViewSet (9 端点)
│   │   │   └── urls.py
│   │   ├── permission/              #   权限管理
│   │   │   ├── models.py            #     Permission, MenuPermissionRelation
│   │   │   ├── views.py             #     PermissionViewSet (10 端点)
│   │   │   └── urls.py
│   │   ├── department/              #   部门管理
│   │   │   ├── models.py            #     Department (自引用树)
│   │   │   ├── views.py             #     DepartmentViewSet (9 端点)
│   │   │   └── urls.py
│   │   ├── log/                     #   操作日志
│   │   │   ├── models.py            #     OperationLog
│   │   │   ├── views.py             #     OperationLogViewSet (4 端点)
│   │   │   └── urls.py
│   │   ├── config_app/              #   系统配置
│   │   │   ├── models.py            #     SystemConfig
│   │   │   ├── views.py             #     SystemConfigViewSet + upload_image
│   │   │   └── urls.py
│   │   ├── dashboard/               #   仪表盘
│   │   │   └── views.py             #     dashboard_stats (聚合查询)
│   │   ├── rag/                     #   RAG 知识库
│   │   │   ├── models.py            #     KnowledgeBase, Document, DocumentChunk
│   │   │   ├── views.py             #     KB/文档/问答视图
│   │   │   ├── services/            #     LLM/向量/文档处理服务
│   │   │   └── urls.py
│   │   └── nl2sql/                  #   NL2SQL 分析（待开发）
│   │       ├── models.py            #     DataSource, QueryHistory
│   │       ├── views.py             #     SQL 查询/历史/数据源视图
│   │       ├── services/            #     NL2SQL 服务/SQL 执行器
│   │       └── urls.py
│   ├── config/
│   │   ├── settings.py              #   Django 配置
│   │   ├── urls.py                  #   总路由注册
│   │   └── wsgi.py
│   ├── utils/
│   │   ├── response.py              #   统一响应 APIResponse
│   │   ├── permissions.py           #   HasPermission 权限校验
│   │   ├── pagination.py            #   标准分页器
│   │   ├── middleware.py            #   操作日志中间件
│   │   ├── tree.py                  #   树形结构工具
│   │   └── excel.py                 #   Excel 导入导出
│   ├── sql/
│   │   └── init.sql                 #   数据库初始化脚本
│   ├── .env                         #   环境变量
│   ├── manage.py
│   └── requirements.txt
│
├── 三人小组开发文档.md              # 本文档（项目总览与分工）
├── RAG知识库模块说明与使用文档.md   # RAG 模块详细文档
├── 开发流程指南.md
├── 开发规范.md
├── 模块划分与开发分工.md
├── 环境搭建指南.md
├── 组织架构模块设计方案.md
└── test_doc.md                      # 测试用文档（RAG 上传测试）
```

---

## 八、核心流程说明

### 8.1 登录与认证流程

```
用户输入账号密码
    → POST /api/user/login
    → 后端验证 → 返回 access_token + refresh_token + user + permissions
    → 前端存入 Pinia Store + localStorage
    → 获取菜单树 → 生成动态路由 → 跳转仪表盘
    → Axios 请求头自动附加 Bearer Token
    → 401 时自动 refresh，并发请求排队等待
```

### 8.2 RBAC 权限模型

```
用户 → 角色（多对多）→ 菜单（多对多）→ 权限（多对多）

前端：
  - 登录后获取 permissions 列表
  - v-permission="'user:delete'" 控制按钮显示
  - authStore.hasPermission('user:edit') 条件渲染

后端：
  - ViewSet 设置 permission_key
  - HasPermission 类校验用户权限列表
  - 超级管理员 *:*:* 直通所有接口
```

### 8.3 动态路由加载流程

```
路由守卫 beforeEach
    → 检查是否有 Token
    → 检查是否已加载菜单（authStore.menuTree）
    → 未加载：调用 getMenuTree() 获取菜单树
    → buildRoutesFromMenu() 递归遍历菜单树
    → componentMap[path] 匹配 Vue 组件
    → router.addRoute() 注册路由
    → next(to) 重新进入目标路由
```

### 8.4 RAG 知识库流程

```
上传文档 → 文档解析(PDF/DOCX/TXT)
    → 文本分块(langchain TextSplitter)
    → 向量化(通义千问 Embedding)
    → 存入 ChromaDB

用户提问 → 问题向量化
    → ChromaDB 相似度检索 Top-K
    → 构建 Prompt（系统提示 + 检索上下文 + 用户问题）
    → 通义千问 LLM 生成回答
    → SSE 流式返回前端
```

### 8.5 NL2SQL 分析流程（待开发）

```
用户输入自然语言问题
    → 选择目标数据源
    → 后端获取数据源表结构（DDL 元数据）
    → 构建 Prompt（系统提示 + 表结构 + 用户问题）
    → 调用 LLM（通义千问/DeepSeek）生成 SQL
    → SQL 安全校验（禁止 DROP/DELETE/UPDATE/INSERT，只允许 SELECT）
    → 执行 SQL 并限制返回行数（默认 LIMIT 100）
    → 前端展示表格结果 + SQL 代码高亮
    → 记录查询历史（支持收藏/重新执行）
```

---

## 九、开发规范

### 9.1 分支策略（Git Flow 简化版）

| 分支 | 用途 | 命名规范 |
|------|------|---------|
| `develop` | 开发主分支 | — |
| `feature/xxx` | 个人功能分支 | `feature/zhangsan`, `feature/lisi` |
| `release/vX.X` | 发布分支 | `release/v1.0` |
| `main` | 生产分支 | — |

### 9.2 Commit 规范

```
<type>(<scope>): <subject>

type: feat | fix | docs | style | refactor | test | chore
scope: user | role | menu | dept | permission | log | config | rag | nl2sql | dict | student | auth | layout
```

### 9.3 编码规范

| 项目 | 规范 |
|------|------|
| Python | PEP 8, 4 空格缩进, 双引号字符串 |
| Vue | `<script setup lang="ts">` + Composition API |
| TypeScript | 严格模式, 接口命名 PascalCase |
| CSS | `<style scoped>`, BEM 可选 |
| 文件命名 | Vue 组件 PascalCase, 工具文件 camelCase |

### 9.4 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

分页响应：
```json
{
  "code": 200,
  "data": {
    "records": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "totalPages": 5
  }
}
```

---

## 十、环境搭建

### 10.1 后端启动

```bash
cd Internship-core
python -m venv venv
venv\Scripts\activate           # Windows
pip install -r requirements.txt
cp .env.example .env            # 编辑数据库等配置
python manage.py migrate
python manage.py runserver 8000 --noreload
```

### 10.2 前端启动

```bash
cd Internship-Pre
pnpm install                    # 或 npm install
pnpm run dev                    # http://localhost:3000
```

### 10.3 环境变量（.env）

```env
# 数据库
DB_ENGINE=mysql                 # mysql 或 sqlite3
DB_NAME=internship
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=127.0.0.1
DB_PORT=3306

# Redis
REDIS_URL=redis://127.0.0.1:6379/0

# RAG（可选，知识库 AI 问答需要）
# 注册地址: https://bailian.console.aliyun.com/
# 同一个 API Key 同时用于 LLM 问答和文档向量化
DEEPSEEK_API_KEY=sk-你的百炼API-Key
DEEPSEEK_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DEEPSEEK_CHAT_MODEL=qwen-turbo
DASHSCOPE_API_KEY=sk-你的百炼API-Key

# RAG 可选配置
CHROMA_PERSIST_DIR=Internship-core/chroma_data
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=100
RAG_TOP_K=5
RAG_MAX_FILE_SIZE_MB=20
```

---

## 十一、联调与测试

### 11.0 相关文档索引

| 文档 | 说明 |
|------|------|
| `RAG知识库模块说明与使用文档.md` | RAG 模块详细设计、环境配置、使用指南 |
| `组织架构模块设计方案.md` | 组织架构模块设计方案 |
| `模块划分与开发分工.md` | 模块划分与开发分工 |
| `开发流程指南.md` | 开发流程与规范 |
| `开发规范.md` | 编码规范 |
| `环境搭建指南.md` | 环境搭建步骤 |

### 11.1 前后端联调

- 前端 Vite 配置 `/api` 代理到 `http://localhost:8000`
- 开发时同时启动前后端即可
- Swagger API 文档：`http://localhost:8000/api/docs/`

### 11.2 测试建议

| 类型 | 工具 | 覆盖目标 |
|------|------|---------|
| API 测试 | Swagger UI / Postman | 全部接口 |
| 前端测试 | 手动 + 浏览器 DevTools | 核心页面流程 |
| 权限测试 | 不同角色账号 | RBAC 全链路 |

---

## 十二、NL2SQL 分析模块设计（待开发）

### 12.1 功能概述

NL2SQL（Natural Language to SQL）模块允许用户通过自然语言描述查询需求，系统自动将自然语言转换为 SQL 语句，执行后返回结构化数据结果。核心场景：

- 管理人员无需编写 SQL，直接输入"查询本月销售额前10的产品"即可获得结果
- 支持对指定数据源进行安全查询，防止 SQL 注入和危险操作
- 查询历史记录与收藏，便于复用

### 12.2 模块架构

```
┌── NL2SQL 分析 ────────────────────────────────────────┐
│                                                        │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐        │
│  │ 自然语言  │ →  │ LLM 转换  │ →  │ SQL 执行  │        │
│  │  输入框   │    │(通义/DS) │    │(安全校验) │        │
│  └──────────┘    └──────────┘    └──────────┘        │
│       ↓                                ↓               │
│  ┌──────────┐                  ┌──────────────┐      │
│  │ 查询历史  │                  │ 表格/图表展示 │      │
│  │ 收藏管理  │                  │  数据导出     │      │
│  └──────────┘                  └──────────────┘      │
│                                                        │
│  ┌──────────────────────────────────────────┐        │
│  │ 数据源管理：数据库连接 + 表结构元数据浏览   │        │
│  └──────────────────────────────────────────┘        │
└────────────────────────────────────────────────────────┘
```

### 12.3 数据库设计

```sql
-- 数据源配置表
nl2sql_datasource (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    name            VARCHAR(100) NOT NULL,      -- 数据源名称
    db_type         VARCHAR(20) DEFAULT 'mysql', -- 数据库类型
    host            VARCHAR(200),
    port            INT,
    db_name         VARCHAR(100),
    username        VARCHAR(100),
    password_enc    VARCHAR(500),                -- 加密存储
    description     TEXT,
    status          TINYINT DEFAULT 1,           -- 0禁用 1启用
    created_by_id   BIGINT,
    created_at      DATETIME,
    updated_at      DATETIME
);

-- 查询历史表
nl2sql_query_history (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL,             -- 查询用户
    datasource_id   BIGINT,                      -- 使用的数据源
    question        TEXT NOT NULL,               -- 自然语言问题
    generated_sql   TEXT,                         -- 生成的 SQL
    execution_time  FLOAT,                        -- 执行耗时(秒)
    result_count    INT DEFAULT 0,               -- 结果行数
    status          TINYINT DEFAULT 1,           -- 0失败 1成功
    is_favorite     TINYINT DEFAULT 0,           -- 是否收藏
    error_message   TEXT,                         -- 错误信息
    created_at      DATETIME
);
```

### 12.4 API 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/nl2sql/query/` | 自然语言转 SQL 并执行 |
| GET | `/api/nl2sql/history/` | 查询历史（分页+筛选） |
| PUT | `/api/nl2sql/history/{id}/favorite/` | 收藏/取消收藏 |
| DELETE | `/api/nl2sql/history/{id}/` | 删除历史记录 |
| GET/POST | `/api/nl2sql/datasource/` | 数据源列表 / 创建 |
| GET/PUT/DELETE | `/api/nl2sql/datasource/{id}/` | 数据源详情 / 更新 / 删除 |
| GET | `/api/nl2sql/datasource/{id}/tables/` | 获取数据源的表结构 |
| POST | `/api/nl2sql/datasource/{id}/test/` | 测试数据源连接 |

### 12.5 前端页面设计

| 页面 | 组件文件 | 功能 |
|------|---------|------|
| SQL 智能查询 | `views/nl2sql/QueryView.vue` | 自然语言输入框 + SQL 预览 + 结果表格 + 导出 |
| 查询历史 | `views/nl2sql/HistoryList.vue` | 历史查询列表 + 收藏 + 重新执行 |
| 数据源管理 | `views/nl2sql/DataSourceList.vue` | 数据源 CRUD + 连接测试 + 表结构浏览 |

### 12.6 核心流程

```
用户输入自然语言问题
    → 选择目标数据源
    → 后端获取数据源表结构（DDL 元数据）
    → 构建 Prompt（系统提示 + 表结构 + 用户问题）
    → 调用 LLM（通义千问/DeepSeek）生成 SQL
    → SQL 安全校验（禁止 DROP/DELETE/UPDATE/INSERT，只允许 SELECT）
    → 执行 SQL 并限制返回行数（默认 LIMIT 100）
    → 前端展示表格结果 + SQL 代码高亮
```

### 12.7 安全机制

| 安全措施 | 说明 |
|---------|------|
| SQL 白名单 | 只允许 SELECT 语句，禁止 DDL/DML |
| 行数限制 | 默认 LIMIT 100，最大 LIMIT 1000 |
| 超时控制 | SQL 执行超时 30 秒自动终止 |
| 敏感词过滤 | 禁止访问 sys_user.password 等敏感字段 |
| 只读账号 | 数据源连接使用只读数据库账号 |

### 12.8 三人任务分配（解耦版）

> 每个开发人员自主完成自己模块的全链路，不依赖他人注册 componentMap / 菜单 / 图标。

#### 人员 C — NL2SQL 全栈（独立完成，无需等其他人）

| 任务 | 前端 | 后端 | 预估工时 |
|------|------|------|----------|
| Django App 基础结构 | — | `apps/nl2sql/`（models, apps, urls, serializers） | 0.5 天 |
| 数据源管理 CRUD | — | `views.py`, `serializers.py` | 1 天 |
| 数据源连接 + 表结构获取 | — | `services/db_service.py` | 1 天 |
| NL2SQL 核心服务 | — | `services/nl2sql_service.py` | 2 天 |
| SQL 安全校验 + 执行引擎 | — | `services/sql_executor.py` | 1 天 |
| 查询历史 + 收藏 | — | `views.py` | 0.5 天 |
| SQL 智能查询页面 | `QueryView.vue` | — | 2 天 |
| 查询历史页面 | `HistoryList.vue` | — | 1 天 |
| 数据源管理页面 | `DataSourceList.vue` | — | 1.5 天 |
| **componentMap 注册** | `store/auth.ts`（自行添加 nl2sql/*） | — | 0.5 天 |
| **iconMap 注册** | `layout/Layout.vue`（自行添加图标） | — | 0.5 天 |
| **菜单 + 权限数据** | — | `sql/init.sql`（自行添加菜单与权限） | 0.5 天 |

#### 人员 B — 字典管理全栈（独立完成）

| 任务 | 前端 | 后端 | 预估工时 |
|------|------|------|----------|
| 字典类型 + 字典数据 CRUD | `DictList.vue`, `DictForm.vue` | `apps/dict/` | 3 天 |
| componentMap + 菜单数据 | `store/auth.ts` + `sql/init.sql` | — | 0.5 天 |

#### 人员 A — 学生中心 + 用户中心全栈（独立完成）

| 任务 | 前端 | 后端 | 预估工时 |
|------|------|------|----------|
| 学生中心后端 + 前端 | `views/student/` | `apps/student/` | 3 天 |
| 学生中心 componentMap + 菜单 | `store/auth.ts` + `sql/init.sql` + `Layout.vue` | — | 0.5 天 |
| 用户中心 | `views/user-center/` | `apps/user/views.py` | 1.5 天 |
| 用户中心 componentMap + 菜单 | `store/auth.ts` + `sql/init.sql` | — | 0.5 天 |

### 12.9 开发排期建议（解耦并行版）

| 周次 | 人员 A | 人员 B | 人员 C |
|------|---------|---------|----------|
| 第 1 周 | 学生中心后端 + 前端 + 注册 | 字典管理后端 + 前端 + 注册 | NL2SQL 后端：App + 数据源 + SQL 引擎 |
| 第 2 周 | 用户中心 + 注册 + 集成测试 | 字典管理联调 + 集成测试 | NL2SQL 后端：LLM 服务 + 查询历史 |
| 第 3 周 | 联调 + Bug 修复 | 联调 + Bug 修复 | NL2SQL 前端：3 个页面 + 注册 |
| 第 4 周 | 全局集成测试 + Demo 准备 | 全局集成测试 + Demo 准备 | NL2SQL 联调 + 全局集成测试 |

> **三人可完全并行开发**，互不阻塞。每个人负责自己模块的全链路（后端 → 前端 → componentMap → 菜单 → 图标）。公共文件（auth.ts、Layout.vue、init.sql）每人只添加自己模块的部分，不会冲突。

---

## 十三、待扩展功能

| 模块 | 待开发功能 | 优先级 | 负责人 | 是否解耦 |
|------|-----------|--------|--------|----------|
| NL2SQL 分析 | 自然语言转 SQL 查询（详见第十二章） | 高 | 人员 C（全栈独立完成） | ✅ |
| 字典管理 | 数据字典 CRUD（系统管理子菜单） | ✅ 已完成 | 人员 B（全栈独立完成） | ✅ |
| 学生中心 | 学生信息管理 + 成绩管理 | 中 | 人员 A（全栈独立完成） | ✅ |
| 用户中心 | 用户自助服务 + 消息通知 | 中 | 人员 A（全栈独立完成） | ✅ |
| 消息通知 | WebSocket 实时通知 | 低 | — | — |
| 多语言 | i18n 国际化支持 | 低 | — | — |
